# 2026-06-23-1224-passoff-file

## Summary

Documentation/orientation session. Rewrote `CLAUDE.md` from the original
goals-brief into accurate project guidance for the shipped app (purpose, live
URL, commands, architecture, conventions, constraints; fixed the `# CLAUDE.MD`
H1 and the "secpond" typo). Fixed the pointers: `LATEST.pointer` already
resolved; `ACTIVE-PLAN.pointer` was empty and now targets a real plan-of-record.
A detailed route-first design-overhaul plan was found untracked in
`docs/plans/` and is adopted as the active plan. No application code changed
this session.

## Inherited state

### Open PRs

_none_

### Carry-forward from prior passoffs

- [2026-06-23-1210] Make closed (finished) games read-only — still open; now
  folded into the design-overhaul plan (Forecast tab, Phase 3 "clean up score
  edit states"). Decide whether to do the small lock fix now or as part of the
  full Forecast redesign.
- [2026-06-23-1210] Open question: should live (in-progress) games also lock, or
  stay editable as a what-if?
- [2026-06-23-1210] Hard-refresh note: the deployed site already contains the
  visual changes; "no changes" was a browser cache.
- [2026-06-23-1210] Backlog: Kalshi real-time odds tab (also a Non-Goal of the
  current plan).

### Prior passoff chain

- 2026-06-23-1210-passoff-file.md

## Files changed

- Modified: `CLAUDE.md` (rewritten to accurate project guidance),
  `docs/plans/README.md` (points at the active plan).
- Added: `docs/plans/design-overhaul-scope.md` (plan-of-record, see decision
  below), `docs/plans/ACTIVE-PLAN.pointer` (now targets the plan; was empty).
- This passoff: `docs/passoffs/2026-06-23-1224-passoff-file.md` and
  `docs/passoffs/LATEST.pointer` repointed to it.

## Key decisions

- `ACTIVE-PLAN.pointer` now targets `design-overhaul-scope.md` rather than being
  deleted, because a real plan-of-record now exists. Both repo pointers resolve.
- `CLAUDE.md` is now project-guidance (commands, architecture, constraints) per
  the harness CLAUDE.md rules, not a goals/TODO list; open work lives in the
  passoff and the active plan instead.
- Committed `design-overhaul-scope.md`, which was found untracked during the
  workspace sweep. It is a polished, project-specific next-phase spec and is the
  obvious `ACTIVE-PLAN` target; committing it is what makes the pointer correct
  per the user's instruction. Flagged below for confirmation.

## Risks and open questions

- `design-overhaul-scope.md` was authored outside this session (found
  untracked). It is committed as the plan-of-record; confirm it is the intended
  spec and not a draft. Its USA vs England scenario dates were spot-checked
  against the schedule earlier and matched.
- The overhaul is large (route-first Matchup view, Forecast + Bracket tabs,
  date-aware paths, palette refresh, 5 implementation phases) and is not
  started. It supersedes the current two-tab layout.
- The finished-games read-only fix from the prior passoff is now ambiguous under
  the new plan (the Forecast tab redesigns score-edit states). Decide whether to
  ship the small lock fix immediately or fold it into Phase 3.

## Workspace sweep

### Auto-resolved

- Committed the untracked `docs/plans/design-overhaul-scope.md` and pointed
  `ACTIVE-PLAN.pointer` at it (per the user's "ensure all pointers are correct"
  instruction).
- Repointed `docs/passoffs/LATEST.pointer` to this passoff.

### Needs user action

- `.DS_Store` files exist in the working tree but are gitignored (never
  committed). Optional local cleanup: `find . -name .DS_Store -delete`.

## Lessons learned

None new.

## State

- Branch: `main` (passoff lands via `passoff/2026-06-23-1224`)
- Commit: pending
- PR: pending
- Passoff version: v1

## Next actions

1. Execute `docs/plans/design-overhaul-scope.md`, Phase 1 (data correctness)
   first: add a schedule map from the games payload, add `toISODate`, make
   scenario output date-aware, and add the USA vs England scenario+date tests
   listed in the plan §19. Ship via PR + merge.
2. Then Phase 2 (route-first Matchup redesign), Phase 3 (Forecast tab — resolve
   the finished-games read-only / score-edit-state question here), Phase 4
   (Bracket reconciliation with `wc-core.js`), Phase 5 (visual polish, print
   stylesheet, a11y).
3. Confirm `design-overhaul-scope.md` is the intended spec before deep work.
4. Tell the user to hard-refresh the live site to see the deployed visual changes.

## Do not do

- Do not attribute Claude Code as an author in commits or PRs (standing user
  instruction; the Co-Authored-By trailer is intentionally omitted).
- Do not add the Kalshi odds tab in this pass (plan Non-Goal).
- Do not make the Matchup tab depend on completing all group forecasts.
- Do not half-count live in-progress scores; do not let edits or live games
  drive the clinched-winner lock.
- Do not re-add the crossed-swords icon, and do not move the app out of root
  `index.html`.
