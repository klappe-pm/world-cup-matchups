# 2026-06-24-0435-passoff-file

## Summary

Executed the six owner-requested changes recorded in the 2026-06-23-1335
passoff plus the owner's new tab-color direction. The Forecast tab now lists all
twelve groups alphabetically with full W/D/L/GF/GA tables open, final results are
read-only, the discovery section is gone, and fixture badges read FINAL / LIVE /
YOU / the match date / FORECAST. The two top tabs now carry distinct accent
colors: Matchup is medium/light blue, Forecast is light/medium green.

## Files changed

Modified:

- `index.html` — tab color tokens (blue + green); Forecast groups rendered
  alphabetically A–L as plain expanded cards (relevance ordering and the
  collapse/`<details>` machinery removed); W/D/L/GF/GA columns always shown
  (stats toggle removed); final official games read-only (disabled inputs, no
  revert, defensive guard in the input handler); REAL chip renamed FINAL; new
  date chip replaces FORECAST for upcoming games once live data is present;
  state chips aligned via `inline-flex`; "in your matchup", "matchday N of 3",
  and the "Every team, by earliest meeting" discovery section all removed;
  stale Forecast intro copy updated.
- `src/wc-core.js` — enrich-only: group-fixture kickoff date now flows from the
  live feed through `parseGroupGames` → `indexLiveByFixture` →
  `fixtureStatus().dateISO`. Structural path engine untouched.
- `tests/test-core.js` — updated the orientation test for the new `dateISO`
  field and added a fixture-date carry test.

## Key decisions

- The owner's explicit "two tab colors — medium/light blue and light/medium
  green" superseded passoff item 3's generic "apply the section 18 dark
  broadcast-pitch palette." Applied the requested tab accents; the full
  section-18 token migration remains deferred as its own focused visual pass
  (the prior passoff already flagged it as large and risky).
- Discovery section removal was confirmed with the owner before deleting (the
  prior passoff guarded it under "Do not do").

## Risks and open questions

- Item 5 (empty upcoming cell not reprojecting) was **not reproducible**. The
  model and the view both reproject correctly when a value is entered into an
  empty upcoming fixture cell (verified for both home and away cells via a
  DOM-shim harness driving the page's real inline script). It was likely
  resolved by PR #7's handler rewrite. The handler was still simplified
  (removed dead expand/collapse state restoration) and given a read-only guard.
- Group-fixture dates resolve only from the live feed; the bundled snapshot
  carries no team names for group games, so offline the badge falls back to
  FORECAST. Acceptable for a live-primary app.

## Verification

- `npm run test:offline` — 25 passed, 0 failed, 1 skipped (live contract test).
- DOM-shim harness confirmed: groups render A–L; no stats toggle; ext columns
  present; FINAL chip (no old REAL); final games disabled; no relnote/matchday
  text; reprojection on empty-cell edit still works. Live-payload check
  confirmed the date chip resolves (Czech Republic → Czechia, 2026-06-24).

## State

- Branch: `claude/passoff-bugs-tab-colors-ecqv7m`
- Passoff version: v1

## Next actions

- Full section-18 visual palette migration (dark broadcast-pitch tokens + light
  print `@media print`), still deferred — do as a focused pass with visual
  verification.
- Phase 4 Bracket tab (deferred, not owner-blocking).

## Do not do

- Do not attribute Claude Code as an author in commits or PRs (owner preference).
- Do not migrate to a framework or add a build step; keep the vanilla static SPA.
- Do not display dates in any format other than `yyyy-MM-dd`.
- Do not change the structural path engine in `wc-core.js`; only enrich output.
