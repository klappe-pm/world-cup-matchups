# 2026-06-23-1335-passoff-file

## Summary

Shipped Phases 1-3 of the route-first redesign to production. The matchup
finder now answers "can A and B meet, in what round, on what date, what path"
with a date-aware core, a hero answer card, a two-lane convergence route, and a
relevance-sorted Forecast tab. Merged via PR #7 (`9b02c68`) and live on GitHub
Pages. After the merge the owner reviewed the live app and returned six concrete
change requests (below) that are NOT yet implemented; they are the next
session's work. Building was explicitly stopped to record them.

## Files changed

Created:

- `docs/passoffs/2026-06-23-1335-passoff-file.md` (this file).

Modified (already merged to main as PR #7):

- `index.html` — hero card, two-lane route with dates, Forecast tab, fix for the
  `relevantGroups` ReferenceError.
- `src/wc-core.js` — date-aware engine (`toISODate`, `buildScheduleByMatchId`,
  `normalizeRound`, model `setSchedule`, `scheduleFor`, `scenariosWithDates`,
  `earliestAnyWithDates`, `relevantGroups`); structural engine unchanged.
- `tests/test-core.js` — date-parse, schedule, and USA-vs-England date tests.
- `docs/plans/design-overhaul-scope.md` — bumped to v2 with frontmatter and
  revision history.
- `docs/passoffs/LATEST.pointer` — repointed to this passoff.

## Key decisions

- Scoped the implementation workflow to Phases 1-3 plus the styles they need;
  deferred Phase 4 (Bracket tab) and the full color-token migration. Rationale:
  date-first UX is the highest-value fix; Bracket is power-user finish work.
- Embedded a 104-match schedule snapshot in `index.html` so dates render offline
  (the live `/get/games` payload carries all knockout dates but the app must
  work from the bundled snapshot too).
- Merged the code PR to prod on explicit owner direction ("commit to prod"),
  doing the merge server-side via `gh` to avoid local HEAD-flip risk while the
  git-concurrency guard was flagging a stale self-lock.

## Risks and open questions

- The six owner-requested changes (see Next actions) were verified as real
  against the live app but are unimplemented. Item 5 (empty-cell edit not
  reprojecting) is a functional bug, not just a preference.
- Item 6 is partly ambiguous: confirm whether the "Every team by earliest
  meeting" discovery section should be removed entirely or just lose its
  subheaders. Owner called it "useless"; default to removing the whole section
  unless they say otherwise.
- Item 3 (apply the section 18 color system) is a large visual change; do it as
  its own focused pass and visually verify before shipping.

## Workspace sweep

### Auto-resolved

- This session's redesign work was already committed and merged (PR #7) before
  the passoff; working tree is clean. This passoff file is staged per-path.

### Needs user action

- Stale local branch `passoff/2026-06-23-1224` is merged (PR #6) but not reaped.
  Diagnose: `git branch -vv | grep 1224`. Resolve:
  `git branch -d passoff/2026-06-23-1224`.

## Lessons learned

Running the app (headless Chrome via the DevTools Protocol) caught a
`relevantGroups` ReferenceError that blanked the entire Forecast tab and broke
`renderAll` — yet the diff looked complete and all unit tests passed. Verifying
UI changes by observation, not by diff, was load-bearing this session.

## State

- Branch: `passoff/2026-06-23-1335`
- Commit: `ff781fb`
- PR: https://github.com/klappe-pm/world-cup-matchups/pull/8
- Passoff version: v1

## Next actions

Owner-requested changes from the live-app review (all in `index.html` unless
noted). Implement, visually verify each, then ship:

1. Forecast: order group cards ALPHABETICALLY (A..L), not by relevance. Remove
   the relevance sort and the "in your matchup" relevance label.
2. Forecast standings: show W/D/L/GF/GA columns OPEN by default (full table),
   not hidden behind the toggle.
3. Apply the color system the owner provided in `design-overhaul-scope.md`
   section 18.2/18.3 (dark broadcast-pitch tokens + light print `@media print`).
   The app does not yet use it. Verify contrast and the existing look.
4. Forecast: finished/closed (REAL) games must be READ-ONLY — their score inputs
   are currently still overwritable. Disable editing of games with a final
   official result.
5. Forecast bug: entering a value into a new/empty (upcoming) fixture cell does
   NOT reproject the group standings. Fix the input handler at `index.html`
   ~line 521 so filling an unplayed cell updates standings. Reproduce first.
6. Discovery + Forecast label cleanups:
   - Remove the "Could meet USA/England earliest" subheaders, and likely the
     whole "Every team by earliest meeting" section (owner called it useless —
     confirm scope before deleting).
   - Rename the REAL chip to FINAL.
   - Remove the "in your matchup" label.
   - Align the Final and Live pills.
   - Replace the FORECAST badge with the match date (`yyyy-MM-dd`).
   - Remove the "matchday 2 of 3" status text on group cards.

Deferred (not owner-blocking): Phase 4 Bracket tab.

## Do not do

- Do not attribute Claude Code as an author in commits or PRs (owner preference).
- Do not migrate to a framework or add a build step; keep the vanilla static SPA.
- Do not display dates in any format other than `yyyy-MM-dd`.
- Do not change the structural path engine in `wc-core.js`; only enrich its
  output. The match-id logic is validated correct.
- Do not delete the discovery section until the owner confirms item 6 scope.
